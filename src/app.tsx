import * as React from 'react';
import * as ReactDOM from 'react-dom';

/**
 * @file 控制台 app
 */

const Home = () => <div>hello electron!!</div>;

ReactDOM.render(<Home />, document.getElementById('app'));
